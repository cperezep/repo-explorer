import './index.scss';

const EmptyRepositories: React.FC = () => (
  <div className="empty-repositories">
    <h2 className="empty-repositories__title">No repositories found</h2>
  </div>
);

export default EmptyRepositories;
